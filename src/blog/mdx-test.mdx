---
title: MDX Test with some input variables and prefilled code blocks
description: Test for MDX with input variables and prefilled code blocks.
pubDate: '2025-06-08'
slug: mdx-test
tags: [mdx, test, variables, code, prefilled, example]
author: Sebastian Danielsson
draft: true
---

import MDXInteractive from '@components/MDXInteractive.astro';

This is a test of MDX with some input variables and prefilled code blocks.

## Interactive Example

Replace the input fields below with your own values and see how they are rendered in the code block in real-time!

<MDXInteractive
  variables={[
    { id: 'variable1', label: 'Package Manager', defaultValue: 'bun', placeholder: 'bun' },
    { id: 'variable2', label: 'Framework', defaultValue: 'astro', placeholder: 'astro' },
    { id: 'variable3', label: 'Library', defaultValue: 'react', placeholder: 'react' },
  ]}
/>

```shell
variable1 install variable2 --add variable3
```

## Traditional Code Block

Here's how the same command would look in a traditional, static code block:

```shell
bun install astro --add react
```
